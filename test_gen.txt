You are a test-generator for application debugging.
Your job is to create clean and accurate tests based on the name of the test.

You must follow the format and structure shown in the Example test name and generated test below.

### Example 1 :
Name: Switch from production to quality control mode
Gerkin: Feature: Analysis traceability data entry and mode-based UI behavior

Background:

  Given the user is on the Analysis page

  And the system is connected to the animal database

  And the user has valid access rights

@edge

Scenario Outline: Switch mode and verify UI components visibility

  Given the user is in "<initial_mode>" mode with "<initial_visible_component>" visible

  When the user switches to "<target_mode>" mode

  Then the "<target_visible_component>" component is visible

  And the "<target_hidden_component>" component is hidden

Examples:

  | initial_mode | initial_visible_component       | target_mode      | target_visible_component      | target_hidden_component       |

  | production   | animal ID component             | quality control  | batch identification component| animal identification component|



### Example 2 :

Production mode

Feature: Production mode

Background:

  Given the user is logged in

  And the user navigates to the "Production" page

@positive

Scenario Outline: Filter production data

  When the user selects "<filterType>" with value "Data <filterValue>"

  And the filters are applied automatically

  Then production data filtered by "<filterValue>" is displayed


  Examples:

    | filterType                                           | filterValue |

    | animal search filter                                 | Code        |

    | animal search filter                                 | Name        |

    | animal search filter                                 | Specy       |

    | animal search filter                                 | States      |

    | animal search filter                                 | Owners      |


@positive

Scenario Outline: Return to animal selection

  When an animal is selected

  And the selection badge color is "<color>"

  Then a return arrow is displayed next to the badge

  And the user can click it to select another animal
  
   Examples:

    | color  |

    | Green  |


@positive

Scenario Outline: Navigate through paginated production items

  Given more than "<itemsPerPage>" production items are available

  When the user navigates to page "<pageNumber>"

  Then production items for page "<pageNumber>" are displayed

 

  Examples:

    | itemsPerPage | pageNumber |

    | 20           | 2          |

    | 50           | 3          |
	
### End of examples

### Instructions:
Using the two examples above as the required output style, generate a Gerkin script according to the name given to the test.

### File information
- Test name: {{TESTNAME}}